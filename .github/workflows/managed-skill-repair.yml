name: managed skill repair caller

on:
  issues:
    types: [labeled]
  workflow_dispatch:
    inputs:
      dry_run:
        description: Validate hosted configuration without running an agent or publishing.
        required: true
        default: true
        type: boolean

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  managed-repair:
    uses: Ian-Tseng/analyze-project-claims/.github/workflows/managed-skill-repair.yml@1412332c1c5a0e7e25e4afc429063f2381598851
    with:
      policy-path: .github/managed-skill-policy.json
      dry-run: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run }}
    secrets:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
